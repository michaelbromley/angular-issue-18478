/**
 * The test class is suitable for testing that which is under test.
 */
export class TestClass {

    /**
     * This is a doc block comment for the foo() method.
     */
    foo() {
        const paula = 'brillant';
    }
}